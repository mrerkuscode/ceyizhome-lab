# Legacy Web UI

This folder is a legacy PySide/WebChannel prototype and is not loaded by the current desktop app.

The active WebView entrypoint is:

- `src/webui/index.html`
- `src/webui/app.js`
- `src/webui/styles.css`

Keep this folder out of production UI work unless the fallback app is explicitly reactivated.
